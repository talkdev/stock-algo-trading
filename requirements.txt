# STMR - short-term mean reversion (intraday, NSE cash, long-only)
# Deliberately light: indicators are pure Python (no numpy/pandas needed).
requests>=2.31
tzdata>=2023.3        # IANA timezones (required on Windows for zoneinfo)
