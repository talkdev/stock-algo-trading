# JF-OU / NSE 2026 -- runtime dependencies
#
# Verified against: Python 3.13, numpy 2.3.5, pandas 2.2.3, scipy 1.17.1,
# statsmodels 0.15.0, arch 8.0.0, requests 2.33.0
#
# statsmodels and arch are the only two that are not usually preinstalled. The code has
# bounded scipy/manual fallbacks for both, so a missing package degrades the estimate
# rather than crashing the scan -- but install them for the real thing.

numpy>=1.26
pandas>=2.0
scipy>=1.11
requests>=2.31
statsmodels>=0.14      # Augmented Dickey-Fuller (gate G4)
arch>=6.0              # GARCH(1,1) conditional variance (confluence S1)

# Windows has no IANA time-zone database of its own. Without tzdata,
# zoneinfo.ZoneInfo("Asia/Kolkata") raises ZoneInfoNotFoundError at import time and
# the session clock -- and with it every command -- is unavailable. clock.py degrades
# to a fixed +05:30 offset if it is missing, but install it for the real thing.
tzdata>=2023.3         # IANA tz database for Windows (a no-op on Linux/macOS)
